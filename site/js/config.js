// InfoLady API Configuration
const API_URL = window.location.origin; // Dynamically binds to localhost or production domain
// export default API_URL; // if using modules, but global is simpler for existing scripts
